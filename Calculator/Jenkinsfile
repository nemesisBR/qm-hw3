pipeline {
    agent any
        tools {
            maven 'Maven 3.8.4'
            jdk 'jdk8'
        }
    stages {
        stage('Build') {
            steps {
                echo "mvn -B -DskipTests clean package"
            }
        }
    }
}